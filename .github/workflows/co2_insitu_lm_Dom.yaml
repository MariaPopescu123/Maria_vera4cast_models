on:
  #schedule:
   # - cron: '0 15 * * *' ## UNCOMMENT THIS WHEN READY FOR AUTOMATION
  workflow_dispatch:


name: co2_insitu_lm_Dom ## POPULATE THIS WITH YOUR MODEL ID

jobs:
  docker:
    runs-on: ubuntu-latest
    container: rqthomas/vera-rocker:latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    steps:
      - uses: actions/checkout@v3

      # - name: Install
      #   shell: Rscript {0}
      #   run: |
      #     install.packages(c("YOUR NEEDED PACKAGES")) ## POPULATE THIS WITH ANY SPECIFIC PACKAGES YOU NEED

      - name: Generate forecasts
        shell: Rscript {0}
        run: |
          source("model_code/co2_insitu_lm_Dom/co2_insitu_lm_Dom.R")
